# 这个文件的意思是：列在这里的东西，git 不会管，也不会传到 GitHub。
# 用途：避免把临时文件、密钥、依赖包传上去。

# 编辑器产生的配置（每人不同，不该共享）
.vscode/
.idea/

# 系统自动生成的垃圾文件
.DS_Store
Thumbs.db

# 依赖包（体积巨大，且可以从 package.json 重新装）
node_modules/

# 环境变量和密钥 —— 最重要的一条，绝对不能传
.env
.env.local

# 日志文件
*.log
